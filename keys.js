

const glass = {
    name: 'glass',
    color: 'golden',
    price: 15,
    is_cleane: true
};

const keys = Object.keys(glass);
// console.log(keys);
const values = Object. values(glass);
// console.log(values);

// console.log(glass);

const entries = Object.entries (glass);
console.log(entries);